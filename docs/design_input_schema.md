# System 600 Design Input Schema

## Purpose

This folder contains design-instance fixtures used to test System 600 compliance logic.

Design files are not part of the System 600 platform specification. They sit in a separate design / instance layer and reference platform types.

The platform defines:

```text
what System 600 allows